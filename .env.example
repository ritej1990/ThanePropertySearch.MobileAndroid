# Copy to ".env" in this folder and set your machine's LAN IP (Expo Go on a phone cannot use localhost).
# Find IP: Windows `ipconfig` → IPv4 Address under your active adapter.
#
# Use the same host your API listens on; match http vs https to your dev certificate setup.
EXPO_PUBLIC_API_BASE_URL=https://192.168.1.10:44396
